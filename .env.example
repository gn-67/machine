# Spotify app credentials — create an app at https://developer.spotify.com/dashboard
# Used only by scripts/sync-spotify.mjs (runs locally); never shipped to the browser.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
